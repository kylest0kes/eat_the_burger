<img src="public/assets/img/burger.png">
    
<h1>Eat The Burger!</h1>

<h2>Devoured Burgers:</h2>
<ol>
  {{#each burgers}}
    {{#if devoured}}
      <li>{{ burger_name }}</li>
    {{/if}}
  {{/each}}
</ol>

<h2>Burgers To Devour:</h2>
<ol>
  {{#each burgers}}
    {{#unless devoured}}
      <li>{{ burger_name }}</li>
    {{/unless}}
  {{/each}}
</ol>

<form class="create-form">

  <div class="form-group">
    <input type="text" id="burger" name="burger" placeholder="What type of Burger do you want to eat?">
  </div>
  <br>
  <button type="submit">Add Your Burger</button>
</form>
